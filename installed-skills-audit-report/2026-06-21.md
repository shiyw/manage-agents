# 每周 Skill 更新与安全审计报告

日期：2026-06-21  
Skills Manager CLI：`/Users/yi/.local/bin/skills-manager-cli`  
Skills Manager 根目录：`/Users/yi/.skills-manager`  
报告范围：通过 Skills Manager 检查、更新并审计已管理 Skill

## 需要警惕的情况

本次没有发现需要隔离的更新文件；两个子智能体安全审计均返回 PASS。需要警惕的是：`skills update --all` 过程中有若干条 transient `skills repository is busy: update installed skill` 错误，虽然后续 `skills check --all` 已确认所有非本地来源均为 `up_to_date`；另外 `gpt-taste` 更新前显示 `update_available`，更新后为 `up_to_date`，但管理仓库没有对应文件 diff，因此只能记录为“状态已刷新、未确认有文件级变更”。

## 本次更新概览

- CLI 解析：`command -v skills-manager-cli` 命中 `/Users/yi/.local/bin/skills-manager-cli`；固定路径 `/Users/yi/Library/pnpm/bin/skills-manager-cli` 不可执行。
- 更新前状态：69 个 skill、7 个 preset；当前 active preset 为 `dangerous`，skill_count 为 0。
- 更新前 `skills check --all`：`gpt-taste`、`test-driven-development` 显示 `update_available`；`aetherforge-ui-refinement` 是 local source，跳过远端检查。
- 已执行：`skills update --all`。
- 更新后 `skills check --all`：所有非本地来源均为 `up_to_date`；`aetherforge-ui-refinement` 为 `local_only`。
- 实际文件级变更：仅 `/Users/yi/.skills-manager/skills/test-driven-development/SKILL.md` 一行，把 `@testing-anti-patterns.md` 改为 Markdown 相对链接 `[testing-anti-patterns.md](testing-anti-patterns.md)`。
- 未隔离任何文件。

## 按 preset 和来源分类的更新清单

| preset | skill | 来源 | 本次结果 | 审计结论 |
|---|---|---|---|---|
| `superpower` | `test-driven-development` | `skillssh:obra/superpowers/test-driven-development` | 实际文件更新 | PASS；仅文档链接修正，无新增高危行为 |
| `design` | `gpt-taste` | `skillssh:leonxlnx/taste-skill/gpt-taste` | 状态从可更新变为已最新；无 tracked file diff | PASS；纯 UI/design prompt，无隔离信号 |
| `design` | `aetherforge-ui-refinement` | `local:/Users/yi/ws/free-twillot-paperclip/.agents/skills/aetherforge-ui-refinement` | update 输出显示 refreshed，但后续 check 为 `local_only`，非远端更新 | 未作为实际上游更新审计；当前无文件 diff |

未归属 preset：本次实际文件级更新无未归属项。  
未知来源：本次更新无未知来源项。

## 可信作者来源说明

规则上，明确来自 GitHub `yizhou-md/*` 或 `shiyw/*` 的 Skill 视为 Yi 自己写的可信作者来源，不因 GitHub、本机高敏操作或未归属 preset 单独判为供应链风险。本次实际更新项来自 `skillssh:obra/superpowers` 和 `skillssh:leonxlnx/taste-skill`，不属于该可信作者规则；但审计未发现与描述不一致的高危行为。

## 子智能体安全审计摘要

- `test-driven-development` 子审计：PASS。检查 `SKILL.md`、`testing-anti-patterns.md` 和目录文件清单；目录只有两个 Markdown 文件，没有脚本、hooks、MCP/app 配置或额外可执行入口。本次 diff 未引入执行、下载、安装、同步、控制面绕过或 secret 读取。
- `gpt-taste` 子审计：PASS。检查 `SKILL.md`；未发现 shell 执行、安装、删除、同步、提交、全局配置修改、MCP/app/hooks/scripts 配置或 secret 读取。唯一外部资源是运行期 UI 代码可引用 `picsum.photos` 图片 URL；不构成隔离理由。

## 已隔离文件及原因

无。未发现需要禁用、移出 preset、sync 隔离或移动到 quarantine 的更新文件。

## Git commit 结果

- Skills Manager 管理仓库 `/Users/yi/.skills-manager/skills`：已提交 `5dcf3ad chore: 更新并审计已安装技能`；提交内容仅为 `test-driven-development/SKILL.md` 一行链接修正。
- 当前工作区 `/Users/yi/ws/manage-agents`：本报告作为本次自动化产物提交，commit message 为 `chore: 记录技能更新与审计报告`；具体 hash 以 `git log -1` 为准。

## 需要警惕的发现

- `skills update --all` 输出中，`dbs-benchmark`、`full-output-enforcement`、`getnote-note`、`getnote-tag`、`imagegen-frontend-mobile`、`lxp-market-autopsy`、`paperclip-company-packages` 曾出现 `skills repository is busy: update installed skill`。后续 `skills check --all` 已显示这些 skill 均为 `up_to_date`，但该错误说明更新过程存在仓库锁竞争或内部并发噪音。
- `gpt-taste` 更新前后状态变化无法对应到 tracked file diff。已做只读内容审计并通过，但无法确认到底是 metadata、cache、远端 revision 记录还是等价内容刷新导致状态变化。
- `aetherforge-ui-refinement` 是本地来源，`skills check --all` 标记 `local_only` 且 skipped；不能用远端更新逻辑证明其来源版本。

## 失败或数据缺口

- 未使用 `pnpx skills`、`npx skills`、手动 clone、直接 symlink 或直接写 raw agent skill 目录。
- 未删除 Skill，未修改 agent 配置、Skills Manager 配置、sync mode、Git remote、API key、`AGENTS.md`、`CLAUDE.md` 或 `.codex/config.toml`。
- 未读取或输出 secret、token、cookie、private key、节点 payload、订阅 URL 或完整环境变量。
- 子智能体均已执行并返回；没有子智能体缺失。
