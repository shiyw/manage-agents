# Agent Logs

Codex writes operation logs here for filesystem and note-management work coordinated from `manage-agents`.

For directory-specific work, prefer the managed root's own `.agents/logs/` folder. Use this folder for global index runs, cross-root audits, and work that spans multiple managed roots.
