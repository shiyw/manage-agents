# manage-agents

This workspace manages local agent configuration, local skills, and the Codex-owned note/filesystem index workspace under `.agents/`.

Current Codex filesystem-control rules live in `~/.codex/references/filesystem_control.md`.
